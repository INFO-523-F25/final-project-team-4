### Source Folder 

This folder contains functions and the script for the dashboard. 

#### How to run the dashboard locally. 
Since adding "streamlit" to the requirements.txt file was breaking the git deploy please install before running the app. 
run the commands:

```bash
pip install streamlit

streamlit run src/app.py
```

Follow the link provided in the console to the locally hosted dashboard. 
